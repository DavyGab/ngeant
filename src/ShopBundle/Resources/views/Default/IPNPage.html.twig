{% extends '::layout.html.twig' %}
{% block body %}
    <section id="benefits-line" class="bg-color3 third-padding pattern-bg" style="background-image: url('{{ asset('appbundle/images/pattern-bg4.jpg') }}');">
        <div class="container" style="margin-top: 120px;">
            <h2 class="big-title text-center" data-selector="h2">{{ titre }}</h2>
            <div class="row">
                <div class="col-sm-8 col-md-offset-2">
                    <h3 class="text-center sep-zero-bottom">{{ message }}</h3>
                    <form action="paypzl !">
                        {% for key, value in form %}
                            <input type="hidden" name="{{ key }}" value="{{ value }}">
                        {% endfor %}
                        <button type="submit" id="rent_submit" class="btn btn-lg btn-block btn-primary">Valider la précommande</button>
                    </form>
                </div>
            </div>
        </div>
    </section>
{% endblock %}

{% block javascripts %}
{% endblock %}